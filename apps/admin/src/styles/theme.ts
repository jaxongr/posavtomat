import type { ThemeConfig } from 'antd';

// White-label tokens — sourced per tenant later; defaults here.
export const theme: ThemeConfig = {
  token: {
    colorPrimary: '#0EA5E9',
    colorSuccess: '#16A34A',
    colorError: '#EF4444',
    colorWarning: '#F59E0B',
    borderRadius: 8,
    fontFamily: 'Outfit, -apple-system, Segoe UI, Roboto, sans-serif',
  },
};
